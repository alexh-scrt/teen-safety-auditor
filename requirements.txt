# requirements.txt — Pinned pip dependencies for reproducible installs
# Generated for teen_safety_auditor v0.1.0
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# OpenAI SDK
openai>=1.30.0

# Templating
jinja2>=3.1.4

# Form parsing
python-multipart>=0.0.9

# Data validation
pydantic>=2.7.0

# Environment variable management
python-dotenv>=1.0.1

# HTTP client (also used by FastAPI TestClient)
httpx>=0.27.0

# Testing (include here so CI can do a single pip install -r requirements.txt)
pytest>=8.2.0
pytest-asyncio>=0.23.0
